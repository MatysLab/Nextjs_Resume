/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', 'images.unsplash.com', 'caimages.collectors.com', 'randomuser.me'],
    },
}

module.exports = nextConfig 